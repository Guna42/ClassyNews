# Frontend Dockerfile - Nginx Reverse Proxy (Optional)
# This serves as a reverse proxy to the backend Flask app
# Use this if you want to separate frontend (port 80) from backend (port 5000)

FROM nginx:alpine

# Remove default nginx config
RUN rm /etc/nginx/conf.d/default.conf

# Copy custom nginx configuration
COPY frontend/nginx.conf /etc/nginx/nginx.conf

# Expose port for Nginx
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
